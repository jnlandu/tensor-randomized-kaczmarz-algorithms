name: Tests

on:
  push:
    branches: [ dev]
  pull_request:
    branches: [ dev]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11"]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Fallback: pre-install tensor_toolbox from Git since it's not on PyPI yet.
        pip install "git+https://github.com/jnlandu/tensor-tensor-toolbox-in-python.git@ded1e12e81735b853b488ac4062eb99f3b1ab08c"
        pip install -e ".[test]"
    
    - name: Run tests with pytest
      run: |
        pytest -v --cov=trk_algorithms --cov-report=xml --cov-report=term
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-${{ matrix.os }}-py${{ matrix.python-version }}
        fail_ci_if_error: false
